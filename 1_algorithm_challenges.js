// Give an array integers, replace all the occurrences of element to replace with subtraction elem.

// Example
// 	For inputArray = [1, 2, 1], element to Replace = 1 and subtractionElem = 3, the output should be arrayReplace (inputArray, elementToReplace, subtractionElem) = [3, 2, 3}.
// Input / output
// [input] array.integer inputArray

// Code Solution: 

// let arrayReplace = function(inputArray, elementToReplace, subtractionElem){
    
//     inputArray.forEach((element, index) => {
//        if(element === elementToReplace){
//         inputArray[index] = subtractionElem
//        }
//     });
//     console.log(inputArray)
// }

// arrayReplace([1,2,1],1,3);


